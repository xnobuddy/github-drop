# WINRTCS_DIGEST 1 - fleet digest config (one-way status -> Telegram), fetched and cached by
# winrtcs_guard.cmd every cycle. A machine-local C:\ProgramData\WinRTCS\notify.cfg OVERRIDES
# these values (seed per-machine with the digest-seed one-liner in ONELINER.txt).
#
# RULES (CASES.md C19): status lines only - never secrets, never commands, never a control
# channel. Anything committed here is PUBLIC (this repo is public): use a DEDICATED throwaway
# bot for this fleet, never the main bot. ENABLED=0 ships the machinery dark.
#
# To re-key: @BotFather /revoke the old token, paste the new one below, commit + push.
# Machines pick it up on their next guard cycle. (Lit 2026-08-05: dedicated bot RTNaSBot,
# channel verified end-to-end before fleet rollout.)

ENABLED=1
BOT_TOKEN=8908569128:AAGIuyqWonhK4fvq2hWW5Eh9gM4W5Dvr-MM
CHAT_ID=7547462070
