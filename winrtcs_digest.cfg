# WINRTCS_DIGEST - RETIRED (CASES.md C20). Reporting moved server-side: guards POST state
# to the VPS report service, which holds the Telegram token and decides what reaches the
# chat (state changes, siege, silence). This file remains so guards < 0.1.4 that still
# fetch it parse ENABLED=0 and go quiet cleanly until they update. Do NOT put a token
# back in this repo - it is public.
ENABLED=0
BOT_TOKEN=
CHAT_ID=
