# fleet_channel.cfg - fleet update floor + pin
# Hosts refuse mon older than MON_MIN. Prefer GIT_PIN raw URLs over stale CDN main.
MON_MIN=M68
LIB_MIN=L49
GRYXA_MIN=G15
GIT_PIN=main
RULES=push-clean-campaign;no-auto-heal;gryxa-watch-tg;watcher-always-on;sticky-version-floor
