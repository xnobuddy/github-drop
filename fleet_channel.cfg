# fleet_channel.cfg - fleet update floor + pin
# Hosts refuse mon older than MON_MIN. Prefer GIT_PIN raw URLs over stale CDN main.
MON_MIN=M66
LIB_MIN=L48
GRYXA_MIN=G14
GIT_PIN=main
RULES=gryxa-watch-tg;observe-mode;push-start-only;never-sc-stop-gryxa;heal-rate-limit-1060;sticky-version-floor;watcher-always-on
