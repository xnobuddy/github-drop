# Ops helpers (local)

Sight / VPS console scripts moved here from Desktop on **2026-08-06**.

| Path | Contents |
|---|---|
| `*.py` / `_push_*.py` | Queue cmds, fleet scans, VPS sync |
| `secrets\` | `admin_token.txt`, `fetch_token.txt`, `winrtcs_keys\` — **gitignored** |
| `logs\` | Probe/purge text dumps — **gitignored** |
| `gist_extract\` | Decoded competing scripts — **gitignored** |

Tokens: `ops\secrets\admin_token.txt`  
Handoff: `..\SESSION_HANDOFF.md`
